apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-aws
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  package: 'registry.upbound.io/crossplane/provider-aws:{{.Values.spec.aws.version}}'
  controllerConfigRef:
    name: provider-aws-controller-config
