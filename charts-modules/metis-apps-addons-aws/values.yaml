spec:
    awsNodeTerminationHandler:
        enabled: true
    albIngressController:
        enabled: true
    aws:
        enabled: true
    destination:
        clustername: test
        server: https://kubernetes.default.svc # agocd registered server address
        name: test
        project: kube-addons-aws # agocd project name
        syncPolicy:
