spec:
    vpa:
        enabled: true
    vaultInjector:
        enabled: true
    reloader:
        enabled: true
    reflector:
        enabled: true
    kubernetesExternalSecrets:
        enabled: true
    kubecleanup:
        enabled: true
    kubedownscaler:
        enabled: true
    kubedescheduler:
        enabled: true
    gatekeeper:
        enabled: true
    destination:
        clustername: test
        server: https://kubernetes.default.svc
        name: test
        project: kube-addons
        syncPolicy:
