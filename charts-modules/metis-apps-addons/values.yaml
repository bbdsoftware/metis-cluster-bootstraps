spec:
    vpa:
        enabled: true
    vaultInjector:
        enabled: true
    reloader:
        enabled: true
    reflector:
        enabled: true
    kubernetesExternalSecrets:
        enabled: true
    kubecleanup:
        enabled: true
    kubedownscaler:
        enabled: true # installs the https://hahow-helm-charts.storage.googleapis.com/ chart
    kubedescheduler:
        enabled: true # installs the https://kubernetes-sigs.github.io/descheduler/ chart
    gatekeeper:
        enabled: true
    kubevela:
        enabled: true
    aws:
        enabled: true
    destination:
        clustername: test
        server: https://kubernetes.default.svc
        name: test
        project: kube-addons
        syncPolicy:
