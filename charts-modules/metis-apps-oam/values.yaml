spec:
    kubevela:
        enabled: true
    aws:
        enabled: true
    destination:
        clustername: test
        server: https://kubernetes.default.svc
        name: test
        project: vela-system
        syncPolicy:
