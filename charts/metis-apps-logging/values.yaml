spec:
    fuentdcloudwatch:
        enabled: true
    loki:
        enabled: true
    lokiDistributed:
        enabled: false
    destination:
        clustername: test
        server: https://kubernetes.default.svc
        name: test
        project: kube-addons-logging
