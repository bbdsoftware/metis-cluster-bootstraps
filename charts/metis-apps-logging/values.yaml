spec:
    fuentdcloudwatch:
        enabled: true
    loki:
        enabled: true
    promtail:
        enabled: true
    destination:
        clustername: test
        server: https://kubernetes.default.svc
        name: test
        project: kube-addons-logging

